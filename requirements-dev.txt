homeassistant>=2024.12.0
pytest-homeassistant-custom-component>=0.13.190
pytest>=8.3.0
pytest-asyncio>=0.24.0
# Matches required-version in pyproject.toml; formatting output changes
# between ruff releases, so CI and the IDE must agree.
ruff>=0.16.0
mypy>=1.14.0
